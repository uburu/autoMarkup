#include "Markup.hpp"

int Markup::MarkupSentences(const vectorized_sent_t &firstSent,
                            const vectorized_sent_t &secondSent) {

}

int Markup::MarkupTexts(const vectorized_text_t &firstSent,
                        const vectorized_sent_t &secondSent,
                        size_t firstWindowSize, size_t secondWindowSize) {
    return 0;
}
